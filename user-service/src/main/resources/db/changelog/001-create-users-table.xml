<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="001" author="powerbank">
        <createTable tableName="users">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="phone" type="VARCHAR(20)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="status" type="VARCHAR(20)" defaultValue="ACTIVE">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMPTZ">
                <constraints nullable="true"/>
            </column>
            <column name="updated_at" type="TIMESTAMPTZ">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <!--
            Phone is the primary lookup key during auth (OTP verify + /v1/me).
            Index here avoids sequential scans on a table that will be queried
            almost exclusively by phone rather than by surrogate ID.
        -->
        <createIndex tableName="users" indexName="idx_users_phone">
            <column name="phone"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
